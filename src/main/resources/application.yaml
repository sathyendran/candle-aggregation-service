
server:
  port: 8081



spring:
  application:
    name: candle-aggregation-service

  threads:
    virtual:
      enabled:true

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: candle-aggregator
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
    properties:
      spring.json.trusted.packages: "*"
      spring.json.value.default.type: com.multibankfx.model.BidAskEvent

  datasource:
    url: jdbc:postgresql://localhost:5432/candle_service
    username: postgres
    password: password

  sql:
    init:
      mode: never